=== FILE: m.dc ===
module m;

public struct Node {
    Node* next;
    Node** prev;
}

public Node* head;

=== EXPECT-TYPES FOR: m ===
Module m
  Struct Node
    Field next: ptr(struct Node)
    Field prev: ptr(ptr(struct Node))
  Var head: ptr(struct Node)

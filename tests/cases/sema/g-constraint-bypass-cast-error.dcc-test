=== FILE: main.dc ===
module m;

using IsInt(T) = compiles(T x) {
    x + 1;
};

struct X(T) if IsInt(T) {
    T val;
}

struct Bad {}

void f() {
    Bad b;
    b as X(Bad);
}

=== EXPECT-ERRORS ===
main.dc:15: template constraint not satisfied for `X`

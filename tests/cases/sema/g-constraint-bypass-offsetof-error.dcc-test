=== FILE: main.dc ===
module m;

using IsInt(T) = compiles(T x) {
    x + 1;
};

struct X(T) if IsInt(T) {
    T val;
}

struct Bad {}

void f() {
    offsetof(X(Bad), val);
}

=== EXPECT-ERRORS ===
main.dc:14: template constraint not satisfied for `X`

=== FILE: main.dc ===
module m;

struct Box(T) {
    T value;
}

using HasValue(T) = compiles(T x) {
    x.value;
};

void use_concrete(Box(i32) b) if HasValue(Box(i32)) {}

void test() {
    Box(i32) b;
    b.value = 42;
    use_concrete(b);
}

=== FILE: main.dc ===
module m;

void fn(i32 x) {}

void expand_index(T...)(T args) {
    fn(args.0...);
}

void caller() {
    expand_index!(i32, f64)(0, 1.0);
}

=== EXPECT-ERRORS ===
main.dc:6: pack-index expression cannot be expanded

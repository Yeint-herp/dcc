=== FILE: main.dc ===
module m;

struct Box(T) {
    T value;
}

using HasValue(T) = compiles(T x) {
    x.value;
};

void use_dependent(T)(Box(T) b) if HasValue(Box(T)) {}

void test() {
    Box(i32) b;
    b.value = 42;
    use_dependent(b);
}

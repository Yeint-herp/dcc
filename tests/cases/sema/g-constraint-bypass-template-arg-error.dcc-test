=== FILE: main.dc ===
module m;

using IsInt(T) = compiles(T x) {
    x + 1;
};

struct X(T) if IsInt(T) {
    T val;
}

struct Bad {}

struct Outer(T) {
    T value;
}

void f() {
    Outer(X(Bad)) o;
}

=== EXPECT-ERRORS ===
main.dc:18: template constraint not satisfied for `X`

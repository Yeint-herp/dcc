=== FILE: main.dc ===
module m;

struct Box(T) {
    T value;
}

using HasValue(T) = compiles(T x) {
    x.value;
};

void use(T)(T x) if HasValue(T) {}

void test() {
    Box(i32) b;
    b.value = 42;
    use(b);
}

=== FILE: main.dc ===
module m;

using IsInt(T) = compiles(T x) {
    x + 1;
};

struct X(T) if IsInt(T) {
    T val;
}

struct Bad {}

void f() {
    i32 flag = 0;
    static if compiles {
        X(Bad) a;
    } {
        flag = 1;
    } else {
        flag = 2;
    }
}

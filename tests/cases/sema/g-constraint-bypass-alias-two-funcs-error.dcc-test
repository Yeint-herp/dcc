=== FILE: main.dc ===
module m;

using IsInt(T) = compiles(T x) {
    x + 1;
};

struct X(T) if IsInt(T) {
    T val;
}

struct Bad {}

using A = X(Bad);

void f1() {
    A a;
}

void f2() {
    A b;
}

=== EXPECT-ERRORS ===
main.dc:16: template constraint not satisfied for `X`
main.dc:20: template constraint not satisfied for `X`
